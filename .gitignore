# ── Secrets / env ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.local
.htpasswd*
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
credentials/
*token*
*secret*

# ── Generated / runtime ──────────────────────────────────────
*.log
logs/
**/logs/
*.pid
*.bak
*.backup
*.tmp

# ── OS / editor junk ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.vscode/
.idea/
.cursor/

# ── Dependencies / build ─────────────────────────────────────
node_modules/
**/node_modules/
dist/
build/
*.tsbuildinfo

# ── OMC / agent caches ───────────────────────────────────────
.omc/
.claude/
CLAUDE.local.md

# ── Local-only deploy bundles ────────────────────────────────
deploy/
remote-sftp-folder-view.html

# ── Active configs (may contain operator-edited live values) ─
# Templates are tracked; active/ may hold a working copy. Track
# only documented examples, never live secrets.
config/active/*.private
config/active/.env.*
